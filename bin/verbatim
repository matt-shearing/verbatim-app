#!/usr/bin/env python3
"""Launcher for the Verbatim CLI (runs from a source checkout, no install)."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from verbatim.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
